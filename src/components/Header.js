import React, { Component, Fragment } from "react";

const Header = ({ title }) => (
  <nav className="nav-wrapperlight-blue darken-3">
    <a className="brqand-logo center">{title}</a>
  </nav>
);

export default Header;
